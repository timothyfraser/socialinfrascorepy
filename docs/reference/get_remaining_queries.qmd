# get_remaining_queries { #socialinfrascorepy.get_remaining_queries }

```python
get_remaining_queries(client)
```

Get remaining monthly query quota.

## Parameters {.doc-section .doc-section-parameters}

| Name   | Type     | Description                                                                                              | Default    |
|--------|----------|----------------------------------------------------------------------------------------------------------|------------|
| client | SIClient | A client from :func:`~socialinfrascorepy.client` authenticated with :func:`~socialinfrascorepy.sign_in`. | _required_ |

## Returns {.doc-section .doc-section-returns}

| Name   | Type             | Description                                                     |
|--------|------------------|-----------------------------------------------------------------|
|        | pandas.DataFrame | A DataFrame with subscription tier and remaining query metrics. |

## Examples {.doc-section .doc-section-examples}

```python
>>> remaining = si.get_remaining_queries(authed)
```